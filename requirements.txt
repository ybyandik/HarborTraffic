# Detection / vision
ultralytics==8.3.0
sahi>=0.11.36
lapx>=0.5.2
opencv-python==4.10.0.84
numpy==1.26.4
torch

# Weather ingest (stream 2)
requests>=2.31.0
pyyaml>=6.0

# Kafka
confluent-kafka>=2.5.0

# Spark analytics (Kafka → Parquet)
pyspark>=3.5.0

# ML / Parquet analysis
pyarrow>=15.0.0
scikit-learn>=1.4.0
joblib>=1.3.0

# Stream tools (YouTube live via yt-dlp pipe)
yt-dlp
yt-dlp-ejs

# NOTE: GStreamer + PyGObject are system packages:
#   sudo apt install python3-gi gstreamer1.0-plugins-good \
#       gstreamer1.0-plugins-bad gstreamer1.0-libav
